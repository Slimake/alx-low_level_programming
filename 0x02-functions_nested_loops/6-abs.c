#include "main.h"
#include <stdio.h>

/**
 * _abs - computes absolute value of integer
 * @num: paramter
 *
 * Return: Always 0.
 */

int _abs(num)
{
	if (num >= 0)
		return (num);
	else
		return (-(num));
}
